#include "main.h"

/**
 * swap int - swaps the values of two integers
 * @ a @ b : characters to check.
 *
 * swaps the values of two integers
 */
void swap_int(int *water, int *b)
{
int temp = *a
*a = *b ;
*b = temp;
}
