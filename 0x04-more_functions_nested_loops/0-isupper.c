#include "main.h"

/**
 * isupper - checks if its uppercase
 *
 * Return: Always 0.
 */
 
 int isupper(int c);
 {
        if(c >= 'A' && c <= 'Z')
     {
        return (1);
     }
        else
     {
        return (0);
     }
 }
